# docker-compose.yml
version: '3.8'

services:
  shared-code-editor:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3456:3456"
      - "3457:3457"
    environment:
      - NODE_ENV=production
      - WEBSOCKET_PORT=3457
      - WEBSOCKET_HOST=0.0.0.0
    restart: unless-stopped
    container_name: shared-code-editor
    